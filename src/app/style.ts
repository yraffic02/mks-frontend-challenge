import styled from "styled-components";

export const ContainerHome = styled.main`
  height: calc(100vh - 6.3rem - 2rem);
  width: 100%;

  display: flex;
  align-items: center;
  justify-content: center;
`;
export const ContainerCards = styled.div`
  width: 50%;
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 1rem;
`;
